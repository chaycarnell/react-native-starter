export interface TranslationsMapping {
  AUTH: {
    DESC: string;
  };
  DASHBOARD: {
    DESC: string;
  };
}

export interface Resources {
  en: {
    translation: TranslationsMapping;
  };
  de: {
    translation: TranslationsMapping;
  };
}
